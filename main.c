//https://leetcode.com/problems/palindrome-number/

#include <stdio.h>
#include <stdbool.h>

bool isPalindrome(int x) {
    ddd
}

#define MAX_NUM 50

int main(){
    int N[MAX_NUM +1];
    scanf("%50s", &N );
    if(isPalindrome(N)){
        printf("palindrome");
    }
}